import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#7250ff',
        },
        secondary: {
            main: '#21adbd',
        },
        success: {
            main: '#8de468',
        },
    },
});

export default theme;