import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#80deea',
        },
        secondary: {
            main: '#7250ff',
        },
        success: {
            main: '#8de468',
        },
    },
});

export default theme;